---
import Layout from '../layouts/Layout.astro';
import { pageDoc } from '../lib/content';

const doc = pageDoc('contact', 'contact');
---

<Layout {...doc}>
  <Fragment set:html={doc.main} />
</Layout>
